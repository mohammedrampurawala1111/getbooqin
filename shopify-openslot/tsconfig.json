{
  "include": ["**/*.ts", "**/*.tsx", ".react-router/types/**/*"],
  // Each extension is its own mini-project with its own tsconfig.json,
  // JSX pragma, and node_modules (see extensions/*/tsconfig.json) — this
  // app's config shouldn't reach into them.
  "exclude": ["extensions/**"],
  "compilerOptions": {
    "lib": ["DOM", "DOM.Iterable", "ES2022"],
    "isolatedModules": true,
    "esModuleInterop": true,
    "jsx": "react-jsx",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "resolveJsonModule": true,
    "target": "ES2022",
    "types": ["vite/client"],
    "strict": true,
    "allowJs": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "rootDirs": [".", "./.react-router/types"],
    "paths": {
      "~/*": ["./app/*"]
    },
    "noEmit": true
  }
}
